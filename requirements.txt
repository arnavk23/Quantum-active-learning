# The authoritative dependency list is in pyproject.toml.
# This file is kept only for tools that expect a requirements.txt; it
# mirrors pyproject.toml's [project.dependencies] plus the optional
# "circuit" and "test" extras. Prefer `pip install -e ".[test,circuit]"`.

numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
pandas>=2.0.0
pymatgen>=2023.8.10
matplotlib>=3.7.0

# optional: needed only for src/quantum_al/circuit.py and the NISQ
# feasibility benchmark (pip install -e ".[circuit]")
qiskit>=1.0.0
qiskit-aer>=0.14.0

# optional: needed only to run the test suite (pip install -e ".[test]")
pytest>=7.0.0
